@model MvcStokTakip.Models.Entity.TBLKATEGORILER
@{
    ViewBag.Title = "Update";
    Layout = "~/Views/Shared/_MainLayoutView.cshtml";
}

<h2>Update</h2>

@using (Html.BeginForm("Guncelle", "Kategori", FormMethod.Post))
{
    <div class="form-group">

        @Html.LabelFor(m => m.KATEGORIID);
        @Html.TextBoxFor(m => m.KATEGORIID, new { @class = "form-control", @disabled = "disabled" })
        <br />
        @Html.LabelFor(m => m.KATEGORIAD);
        @Html.TextBoxFor(m => m.KATEGORIAD, new { @class = "form-control" })

    </div>

    <button style="margin-top:10px;" class="btn btn-secondary">Güncelle</button>

}